{
  "name": "aihimeratrading-defi",
  "version": "1.0.0",
  "description": "AIHIMERATRADING DeFi Track Solana certification project: Anchor CRUD + PDA + SPL vault staking for AI trading signals.",
  "scripts": {
    "build": "anchor build",
    "test": "anchor test",
    "lint": "cargo clippy --all-targets --all-features -- -D warnings"
  },
  "devDependencies": {
    "@coral-xyz/anchor": "^0.30.1",
    "@solana/spl-token": "^0.4.8",
    "@solana/web3.js": "^1.95.3",
    "typescript": "^5.5.4",
    "ts-mocha": "^10.0.0",
    "chai": "^4.5.0"
  }
}
